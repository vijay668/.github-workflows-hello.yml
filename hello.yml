---
name: My First Pipeline

on: 
  push:
    branches:
      -main

jobs:
  hello:
      runs-on: ubuntu-latest
      steps:
         -name: print hello
           run: echo "Hello, CI/CD!"

         -name: print date
           run: date

         -name: list files
           run: ls -la

